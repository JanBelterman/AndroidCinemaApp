<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cinema.avans.cinemaapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Log in -->
        <activity
            android:name=".presentation.login.LoginActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.register.RegisterActivity" />

        <!-- Manager -->
        <activity android:name=".presentation.manager.ManagerHubActivity" />
        <activity android:name=".presentation.manager.CreateHallActivity" />
        <activity android:name=".presentation.manager.CreateMovieActivity" />
        <activity android:name=".presentation.manager.CreateShowingConfirmActivity" />
        <activity android:name=".presentation.manager.CreateShowingSelectMovieActivity" />
        <activity android:name=".presentation.manager.CreateShowingSelectHallActivity" />

        <!-- User -->
        <activity android:name=".presentation.user.activities.UserMenuActivity" />
        <activity android:name=".presentation.user.activities.MovieDetailedActivity" />
        <activity android:name=".presentation.user.activities.ShowingsActivity" />
        <activity android:name=".presentation.user.activities.SeatSelectorActivity" />
        <activity android:name=".presentation.user.activities.PayActivity" />
        <activity android:name=".presentation.manager.CreateShowingSelectDateActivity" />
    </application>

</manifest>